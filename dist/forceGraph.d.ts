import type { AdaptiveDrawOptions, DrawOptions, EdgeAppearance, GraphEdgeInput, GraphNodeInput, NodeAppearance, NodeId, RenderSnapshot, SimulationOptions, StepSummary, WasmForceKernel } from "./types.js";
declare const DEFAULT_SIMULATION_OPTIONS: SimulationOptions;
declare function projectPoint(x: number, y: number, z: number, options: DrawOptions): {
    x: number;
    y: number;
    depthScale: number;
};
export declare class ForceGraph<T extends NodeId = string> {
    private readonly options;
    private readonly idToIndex;
    private readonly ids;
    private nodeCapacity;
    private nodeCountValue;
    private x;
    private y;
    private z;
    private vx;
    private vy;
    private vz;
    private ax;
    private ay;
    private az;
    private mass;
    private nodeRadii;
    private nodeColors;
    private activeMask;
    private edgeCapacity;
    private edgeCountValue;
    private edgeSource;
    private edgeTarget;
    private edgeWeight;
    private edgeWidths;
    private edgeColors;
    private tree;
    private nodeIndexes;
    private iteration;
    private currentTheta;
    private wasmKernel;
    private hasNodeAppearanceOverrides;
    private hasEdgeAppearanceOverrides;
    constructor(options?: Partial<SimulationOptions>);
    get nodeCount(): number;
    get edgeCount(): number;
    setWasmKernel(kernel: WasmForceKernel | null): void;
    addNode(id: T, input?: GraphNodeInput): number;
    addEdge(sourceId: T, targetId: T, weight?: number): number;
    addEdge(sourceId: T, targetId: T, input?: GraphEdgeInput): number;
    setNodeAppearance(id: T, appearance: NodeAppearance): void;
    setEdgeAppearance(index: number, appearance: EdgeAppearance): void;
    getNodePosition(id: T): {
        x: number;
        y: number;
        z: number;
    } | null;
    getAdaptiveDrawOptions(width: number, height: number, frameTimeMs: number, baseNodes?: number, baseEdges?: number): AdaptiveDrawOptions;
    step(iterations?: number): StepSummary;
    draw(ctx: CanvasRenderingContext2D, options: DrawOptions): void;
    exportRenderSnapshot(maxNodes?: number, maxEdges?: number): RenderSnapshot;
    nodeAt(index: number): {
        id: T;
        x: number;
        y: number;
        z: number;
        mass: number;
    } | null;
    getX(index: number): number;
    getY(index: number): number;
    getZ(index: number): number;
    getMass(index: number): number;
    private updateActiveSet;
    private applyEdgeAttraction;
    private applyEdgeAttractionZOnly;
    private applyRepulsion;
    private applyGravity;
    private applyNodeAppearance;
    private ensureNodeCapacity;
    private ensureEdgeCapacity;
    private growFloatArray;
    private growFloat32Array;
    private growUint32Array;
    private growUint8Array;
    private prepareNodeIndexList;
    private lineIntersectsViewport;
}
export { DEFAULT_SIMULATION_OPTIONS, projectPoint };
